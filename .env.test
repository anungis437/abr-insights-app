# Test environment variables for integration tests
# These are loaded by vitest for tenant isolation tests
# SECURITY: Never commit real keys! Use .env.local for actual values

# Copy from your .env.local file or Supabase dashboard
NEXT_PUBLIC_SUPABASE_URL=your-project-url
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key

# NOTE: You must set these in .env.local (gitignored) before running tests
